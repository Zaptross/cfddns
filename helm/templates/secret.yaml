apiVersion: v1
kind: Secret
metadata:
  namespace: "{{.Values.namespace}}"
  name: {{include "cfddns.fullname" .}}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | b64enc | quote }}
  {{- end }}
